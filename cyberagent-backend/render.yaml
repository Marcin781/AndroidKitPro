services:
  - type: web
    name: cyberagent-api
    runtime: python
    rootDir: cyberagent-backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
